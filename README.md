# ResistorVision PRO
App nativa generada a partir de `DISEÑO.html` mediante Capacitor + Vite.
## Features
- UI Premium con Glassmorphism.
- Haptics táctiles.
- Lectura de resistencias por cámara.

## Ejecutar Local
1. `npm install`
2. `npm run dev`
3. Para compilar android local: `npx cap sync android` y abrir en Android Studio.
